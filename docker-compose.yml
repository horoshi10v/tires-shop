services:
  api:
    build: .
    container_name: tires_backend
    restart: always
    ports:
      - "8083:8083"
    depends_on:
      - db
      - minio
    env_file:
      - .env
    volumes:
      - ./.minio-data:/app/.minio-data
    networks:
      - edge
      - default

  minio:
    image: minio/minio:latest
    container_name: tires-minio
    restart: always
    environment:
      MINIO_ROOT_USER: admin_tires
      MINIO_ROOT_PASSWORD: super_secret_password
    volumes:
      - ./.minio-data:/data
    command: server /data --console-address ":9001"
    networks:
      - default
      - edge

  db:
    image: postgres:15-alpine
    container_name: tires_shop_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: tires_shop
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - default

  pgadmin:
    image: dpage/pgadmin4
    container_name: tires_shop_pgadmin
    restart: always
    ports:
      - "127.0.0.1:5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    depends_on:
      - db
    networks:
      - default

volumes:
  postgres_data:

networks:
  edge:
    external: true
