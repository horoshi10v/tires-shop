basePath: /api/v1
definitions:
  domain.CreateLotDTO:
    properties:
      brand:
        type: string
      condition:
        enum:
        - NEW
        - USED
        type: string
      initial_quantity:
        type: integer
      model:
        type: string
      params:
        additionalProperties: true
        type: object
      purchase_price:
        type: number
      sell_price:
        type: number
      type:
        enum:
        - TIRE
        - RIM
        type: string
      warehouse_id:
        type: string
    required:
    - brand
    - condition
    - initial_quantity
    - purchase_price
    - sell_price
    - type
    - warehouse_id
    type: object
  domain.CreateOrderDTO:
    properties:
      customer_name:
        type: string
      customer_phone:
        type: string
      items:
        items:
          $ref: '#/definitions/domain.OrderItemDTO'
        minItems: 1
        type: array
    required:
    - customer_name
    - customer_phone
    - items
    type: object
  domain.LotResponse:
    properties:
      brand:
        type: string
      condition:
        type: string
      current_quantity:
        type: integer
      id:
        type: string
      model:
        type: string
      params:
        additionalProperties: true
        type: object
      sell_price:
        type: number
      status:
        type: string
      type:
        type: string
      warehouse_id:
        type: string
    type: object
  domain.OrderItemDTO:
    properties:
      lot_id:
        type: string
      quantity:
        type: integer
    required:
    - lot_id
    - quantity
    type: object
  domain.PnLReport:
    properties:
      cogs:
        description: Cost Of Goods Sold (Purchase price)
        type: number
      profit:
        description: Revenue - COGS
        type: number
      revenue:
        description: Total sales amount
        type: number
    type: object
  domain.UpdateOrderStatusDTO:
    properties:
      status:
        enum:
        - NEW
        - PREPAYMENT
        - DONE
        - CANCELLED
        type: string
    required:
    - status
    type: object
host: localhost:8080
info:
  contact:
    name: Valentyn Khoroshylov
  description: This is a REST API for managing a tires and rims store/warehouse.
  title: Tires Shop CRM API
  version: "1.0"
paths:
  /lots:
    get:
      consumes:
      - application/json
      description: Get a paginated list of lots with optional filtering by brand or
        type.
      parameters:
      - default: 1
        description: Page number
        in: query
        name: page
        type: integer
      - default: 10
        description: Items per page
        in: query
        name: page_size
        type: integer
      - description: Filter by brand name (ILIKE)
        in: query
        name: brand
        type: string
      - description: Filter by status (e.g. ACTIVE, ARCHIVED)
        in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/domain.LotResponse'
            type: array
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: List all active lots
      tags:
      - lots
    post:
      consumes:
      - application/json
      description: Add a new lot to the inventory with details like brand, type, and
        quantity.
      parameters:
      - description: Lot details
        in: body
        name: lot
        required: true
        schema:
          $ref: '#/definitions/domain.CreateLotDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - RoleAuth: []
      summary: Create a new lot
      tags:
      - lots
  /orders:
    post:
      consumes:
      - application/json
      description: Place a new order with customer details and tire items.
      parameters:
      - description: Order details
        in: body
        name: order
        required: true
        schema:
          $ref: '#/definitions/domain.CreateOrderDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - RoleAuth: []
      summary: Create a new order
      tags:
      - orders
  /orders/{id}/status:
    put:
      consumes:
      - application/json
      description: Change the status of an existing order (e.g., NEW, PREPAYMENT,
        DONE, CANCELLED).
      parameters:
      - description: Order ID
        in: path
        name: id
        required: true
        type: string
      - description: New status
        in: body
        name: status
        required: true
        schema:
          $ref: '#/definitions/domain.UpdateOrderStatusDTO'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - RoleAuth: []
      summary: Update order status
      tags:
      - orders
  /reports/pnl:
    get:
      consumes:
      - application/json
      description: Calculates total revenue, COGS, and profit based on DONE orders.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/domain.PnLReport'
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - RoleAuth: []
      summary: Get Profit & Loss Report
      tags:
      - reports
securityDefinitions:
  RoleAuth:
    in: header
    name: X-User-Role
    type: apiKey
swagger: "2.0"
